<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Transport Fee Receipt</title>
  <style>
    body { font-family: Arial, sans-serif; font-size: 14px; padding: 20px; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th, td { border: 1px solid #000; padding: 5px; text-align: left; }
    .center { text-align: center; }
    .bold { font-weight: bold; }
    .header { display: flex; align-items: center; justify-content: start; }
    .header img { height: 60px; }
    .alignCenter {display: flex; align-items: center; justify-content: center; width: 80%;}
    .footer { font-size: 12px; margin-top: 20px; }
    .no-border td { border: none; }
  </style>
</head>
<body>

  <div class="header">
    <img src="https://i.imgur.com/EuZQ5Gv.png" alt="Logo" />
    <div class="alignCenter">
        <div class="center">
            <h2>ABC Education Trust</h2>
            <p>Kolhapur</p>
            <h3><i>Academic session (<%=academicYear%>) <%= feeType%></i></h3>
        </div>
    </div>
  </div>

  <table class="no-border">
    <tr><td><b>Name:</b> <%= name %></td> <td><b>Std:</b> <%= standard %></td> <td><b>Date:</b> <%= date %></td></tr>
    <tr><td><b>Student ID:</b> <%= studentId %></td> <td><b>Receipt:</b> <%= receiptNo %></td> <td><b>Fee ID:</b> <%= feeId %></td></tr>
  </table>

  <% if (feeType === "Transport Fee") { %>
  <p><b>StopName:</b> <%= stopName %></p>
<% } %>


  <table>
    <tr><th></th><th><%= title %></th><th>Paid (Rs.)</th></tr>
    <% feeItems.forEach(f => { %>
      <tr>
        <td><%= f.title %></td>
        <td><%= f.amount %></td>
        <td><%= f.amount %></td>
      </tr>
    <% }) %>
    <tr class="bold">
      <td>Total</td>
      <td><%= totalAmount %></td>
      <td><%= totalAmount %></td>
    </tr>
  </table>

  <table>
    <tr class="bold">
      <th>Mode</th>
      <th>Transaction No.</th>
      <th>Transaction Date</th>
      <th>Amount (Rs.)</th>
    </tr>
    <tr>
      <td><%= paymentMode %></td>
      <td><%= transactionId %></td>
      <td><%= transactionDate %></td>
      <td><%= totalAmount %></td>
    </tr>
    <tr class="bold"><td colspan="3">Grand Total</td><td><%= totalAmount %></td></tr>
  </table>

  <p class="footer"><b>In favour of ABC Education Trust</b></p>
  <p class="footer">Note: Subject to Realisation of Cheque. Fees once paid will not be refunded. This is computer generated Receipt & does not require signature. Parent Copy</p>

</body>
</html>
